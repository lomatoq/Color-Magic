# 3.5.2

- Optional asynchronous GitHub stable-release checks and explicit installation. SHA256 and internal manifest validation, backup and rollback on replacement failure; restart required.
- Task-based Russian, English and Belarusian guides; installation, Fast Track, materials, rendering, updates and troubleshooting.

# 3.5.1

- Localize actual addon-owned RNA tooltips, including fields, enums and poll explanations, while preserving stored values and callbacks.
- Add Fast Track batch framing, appearance transfer and imported-model preparation using the existing workspace operations.
- Verify 20 EN/BE switches, source-model isolation, original 23 scooter regions, unchanged mesh transforms, both Blender versions and real batch export.
- Fix blank tab captions and avoid showing export errors before batch preparation.

# 3.5.0 — 2026-09-07

- Resolve current model color bindings automatically at export, without changing topology, shader surfaces or requiring legacy Auto Setup.
- Render current appearance without writing color inputs, including models with fixed materials.
- Add addon-local English/Belarusian labels, localized core menus and action-specific hover help.
- Remove legacy panels; retain selective recoloring, zone policy, native variants, studio controls and palette JSON in the main workspace.
- Validate real two-model exports, fixed gold, restoration, duplicate-path refusal, 300 tasks per model, both supported Blender versions, all camera presets and studio recovery.

# 3.5.0 RC16 — 2026-09-07

- Integrate instance creation, varied surfaces, checked-library Autoassign and zone/face assignment into the main workspace.
- Add meaningful readiness, preview, unapplied-palette and changed-surface states.
- Keep instance color inheritance native; isolate assigned copies from library edits and unchecked models. Repeated unchanged Autoassign is a no-op.
- Persist instances and reuse compact material controls without camera-projection scans.

# 3.5.0 RC15 — 2026-09-07

- Restore the user-selected RC5 scooter in a companion scene with its exact 23 zones and original surface assignments; integrate model collections, named palette presets and the preserved vault.
- Verify material transfer preserves zones, reflected/repeated pairs and vault materials; first transfer 0.58 s, repeat 0.003 s on this case.
- Fix model preparation reusing a collection from another scene; preserve that scene's memberships.
- Keep dense-mesh seam reconstruction experiments out of the release after the user clarified which original result to restore.

# 3.5.0 RC14 — 2026-09-06

- Preserve raw islands instead of a disconnected remainder; restore sharp-edge inputs and reflected/scaled matching.
- Avoid camera-projection scans during known-family transfer and reuse complex shader captures.
- Keep sub-0.5% details selectable while assigning a conservative common family surface.

# 3.5.0 RC13 — 2026-09-06

- Restore established secondary-region assignment instead of coloring every region except the largest as Accent.
- Track aggregate remainder regions and exclude them from automatic Accent selection and matching.
- Remove unused working material slots without purging original materials or changing effective face assignments. Preserve slots referenced by modifiers/particles.
- Box each model and all its controls separately in the workspace.
- Verify the same scooter partition in before/after native renders: Accent area 82.26% → 19.52%; no scattered remainder-zone coloring. Vault face materials and surfaces are unchanged.

# 3.5.0 RC12 — 2026-09-06

- Avoid baking the previous native palette color into Main-to-Accent reassignment.
- Fix captured-source metallic surfaces being replaced by repeated automatic material assignment.
- Add allocation-free scoped recoloring and durable reversible color preview with debounced palette edits.
- Expose preparation, root selection, missing-zone discovery and optional auto zones in model cards.
- Batch initial family assignment and cache UI region counts; preserve all advanced tools.
- Normalize family/surface names; remove stale RC7 UI version label.
- Verify 42 focused checks and 73 regression checks in Blender 3.6.5 and 5.2.1, including a 20-model isolated queue.
- Rehearse recovery on the user's vault: 48 meshes / 205,372 faces preserve original Metallic values. Scooter: 32 regions, with assignments unchanged during discovery.

# 3.5.0 RC11 — model-scoped appearance workspace

- Replace the guided home with palette/source → explicit recipients → Apply.
- Named palette bundles, editable drafts, immutable source-material snapshots and colors-only/full-material transfer.
- Live deleted-model detection, zone origin, applied palette, pending draft edits, Fixed material count and disabled-action explanations.
- Allocate scoped family sources so unchecked models never receive persistent palette changes.
- Preserve all existing tools in the advanced section.
- Fix partial assignments on an empty material-slot list: unselected faces retain an empty slot.

# 3.4.3 RC10 — readable export folders

- Export into Model / Main (color) / Accent (color) / Model (Main color - Accent color).png.
- Add size subfolders only for multiple requested sizes. Keep collision preflight.
- Preview the actual generated path, including model name and custom filename template.

# 3.4.2 RC9 — selected-part recoloring

- Direct Main / Accent / Keep Color for selected meshes, Empty descendants and faces; no adoption dialog.
- Keep unselected material IDs, original appearance, shaders and texture graphs; persist selection-only palette scope.
- Share reusable native descendants, freeze or reassign individual pieces, and restore Edit Mode.
- Simplify Assignment: selection actions and parent swatches first, automatic library distribution collapsed.

# 3.4.1 RC8 — studio recovery

- Live scene checks replace stale rig-ID readiness after deleting cameras, lights or collections.
- One action rebuilds an incomplete studio; deleted model rows no longer mask a new model.
- The camera block identifies its target model and offers fitting an existing scene studio to a newly selected model.
- Restore maps legacy Eevee engine IDs across Blender 3.6 / 5.2; unavailable third-party engines no longer block cleanup.
- Intensity controls unchanged.
- Fix Export PNG panel draw: Add Size uses its registered resolution_add operator; rendering controls no longer disappear on invalid ADD enum.

# 3.4.0 RC7 — 2026-09-05

- Authored assignments precede geometry inference, including single-material meshes and nested shader/color groups. Explicit independent zone/material replacement, proposals for flat imports, exact initial color calibration, Fixed materials and repeat-safe search.
- Real per-model collections, transform roots, durable hierarchy rollback, independent processing/visibility, automatic discovery and library reuse on additional models.
- Native Principled/Diffuse/Emission/custom targets, tint controls and descriptive child names; all imported descendants appear in the library.
- Multiple palettes per family, full Cartesian/pair/current export, simultaneous scale/absolute sizes, nested color/size paths, count and collision checks.
- 14 views, custom angles, 0–45% margins, six light presets and optional family-linked backdrop.
- Production vault repaired from its retained original meshes; six spurious one-face regions removed, gold remains Fixed. Separate review blend, real before/after renders and reopen/rollback checks.

# 3.3.0 RC6 — 2026-09-05

- Native shared Prime Main / Accent shader sources; color inheritance persists without the add-on.
- Full separate palettes restored, including JSON save/load and export combinations.
- Child creation assigns by default; visible library shows usage and surface controls.
- Actual roughness/metallic edits update generated names; deliberate renames are retained.
- Zone labels remain independent of material slots; identical generated base copies consolidate.
- Add Selected Model + Find Zones reuses ready materials with optional automatic assignment.
- Family/disabled-child controls rewire native inheritance; ordinary saves retain selected colors.
- Shared snapshots deduplicated; rollback fixes Blender 3.6 ID renaming and unused-library isolation.
- Saved shader links verified by rendering the scooter with the add-on unregistered.

# 3.2.2 RC5 — 2026-09-05

- Five compact workflow blocks; explicit zone creation and distribution review.
- Strongly similar rotated, mirrored and uniformly scaled parts share variants.
- Preserve authored families, protected faces and existing region labels.
- Model-independent family/surface material names, legacy name cleanup and readable numbering.
- Correct BMesh zone count and repeated highlighting in Edit Mode.
- Repeatable export self-test output directories.
- Verified on actual Blender 3.6.5 and 5.2.1 LTS; see VALIDATION.md.

# 3.2.0-rc.3 — Studio + Regions

- Fix generic RNA is_array access; numeric-only capability detection, enum flags and strict descriptors.
- Guard Prepare and run_auto snapshot exceptions; stop structural rollback while in Edit Mode.
- Add optional reversible orthographic camera, Key/Fill/Rim softboxes, World, transparent frame.
- Per-icon fitting with actual camera frame conventions; owned-rig pose journals and cleanup.
- Avoid inherited crop, non-square pixels and sequencer overriding a requested studio camera.
- Add conservative connected-mesh material-region proposals without topology edits.
- Add seam/crease/neck analysis, convex DETAIL mode, explicit abstention and 120k-face limit.
- Add region highlight and selected-face assignment; preserve protected / manually assigned materials.
- Expand native scratch test to actual Prepare operator, real RNA, one-mesh regions and studio render.
- Test evidence distinguishes host unit/doubles from unexecuted native Blender checks.

---

# 3.1.1-rc.2 — Guided RC2

- Reproduce RC1 import error using actual RC1 and 3.0.1 sources with API doubles.
- Reload only this add-on's complete module bundle; also remove stale package attributes.
- Verify module SHA-256 inventory at import; provide a clear reinstall error on a mixed/incomplete bundle.
- Guard active render updates and duplicate enabled installations.
- Default native three-step Model / Colors / Export workflow, Belarusian and English hints, Advanced retained.
- Explicit selected scope: multiple selected mesh parts can form one logical icon without reparenting the scene.
- Load reference, analyze, generate and preview through one command; retain hand-edited looks.
- Debounced main-thread preview on swatch edits and look selection; cancel on save/load/undo/unregister/render.
- Distinguish palette swap from material-role swap. Manual Fixed can be corrected explicitly; ! stays protected.
- Non-destructive simple export task overrides, exact count/size, explicit confirmation, no silent preset replacement.
- Comparison-editor return path and stale-thumbnail notice.
- Inline state-aware help, recovery guidance and one-click local diagnostic copy.
- CLI Blender gate error-handler signature corrected. No real Blender gate was executed in this environment.

---

# 3.1.0 RC1 — 2026-09-05

This is a substantial implementation over 3.0.1, not a rename-only package.
New: transaction.py, color_graph.py, spatial_reference.py, look_metrics.py,
camera_evidence.py, scene_guard.py, render_session.py, lookbook.py,
contact_sheet.py, export_kernel.py, png_validation.py, batch_export.py,
preflight.py, studio_properties.py, studio_ops.py, studio_ui.py, selftest.py,
and demo.py. Legacy property/operator contracts are retained where feasible.

Added persistent structural stage/rollback; exact raw HDR preview snapshots;
source-aware nested color graphs; bounded spatial reference analysis;
real rendered looks and optional one-step correction; validated atomic output;
safer render lifecycle; dual-location UI; explicit core vs runtime self-tests.

Local tests: see current VALIDATION report. Blender runtime not executed here.
